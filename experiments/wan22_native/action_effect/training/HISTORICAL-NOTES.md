This prepares one controlled action-learning experiment. It starts from the exact original zero-output adapter and copies all 128 original main noise draws, timesteps and RNG records. It keeps the original 947,712-parameter final-block adapter, native FP32 storage/BF16 computation, paired main FM loss, AdamW settings, clipping, window order and final-checkpoint policy.

At the 32 start-0 updates, both commands receive the same existing noise and independent observation. Two frozen feature extracts, one per genuine text context, support four adapter/head predictions. Native guidance is G=N+5*(P-N). The auxiliary predicts the clean endpoint difference as -(Gopen-Gclosed) and compares it with the original open-minus-closed target difference over the four future latents. Its fixed weight is 1. This defines the ideal pure-noise endpoint s=1; it is not the literal solver's initial sigma and not an ordinary k999 FM corruption. The target difference enters the loss only.

The two main half-losses and the auxiliary accumulate gradients before one unchanged clip and AdamW step. The other 96 updates use the original main update; a CPU regression checks exact parameter, gradient and optimizer equality with the frozen routine. All 256 main predictions, 128 auxiliary predictions and 128 clipped-gradient bundles are retained. Checkpoints remain 0,16,...128. The foundation receives no gradients and is checked before/after. Counters distinguish 256 main plus 64 auxiliary feature forwards, 256 main plus 128 auxiliary head outputs, and the two native/two bridge parity calls.

Preparation and readback passed on the actual retained inputs. The four predeclared held-out noise identities are bound by manifest, but their tensors are not loaded or used in training. They remain a separate evaluation package owned by Zeno. The selected proposal requires lower guided contrast error than both zero and old128 on every held-out noise, with positive target alignment. These are held-out noises on one seen scene. The original fixed k506 and visual pair remain separate checks. Only the final checkpoint is evaluated; no extra updates or weight sweep is authorized by a failure.

The new raw auxiliary velocities add 421,724,160 payload bytes, about 402.2 MiB, plus small headers/metadata. The two hidden/time feature bundles total about 201 MiB before head/backward graphs. The extra 64 foundation feature forwards increase that work by 25%; actual runtime and peak memory for this loss are unmeasured. The source-based estimate of roughly 450–550 seconds needs guarded confirmation. Limits remain 900 seconds, 48 GiB host RSS, 60 GiB CUDA reserved memory, and 8 GiB minimum available host/CUDA memory. Provider lifecycle and later 1,800-second visual admission are separate parent responsibilities.

14 focused CPU checks passed, including the complete tiny 128-update path. No pretrained model, CUDA job, cloud operation or training admission was executed or created by this preparation. Original sources and measured artifacts remain unchanged. Draft test/preparation evidence is retained alongside the final source-bound records.

The CLI defaults to preparation. Runtime preparation consumes --completed-probe, --cache-run, --text-directory, --fixed128-prepared, --diagnostic-prepared, --cpu-report, --profile spatial and --output. Execution additionally requires --execute, the exact prepared directory, completed original14 diagnostic, weight directory and an explicit new effect128 parent decision. It does not automatically launch another stage.
