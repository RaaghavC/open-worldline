This is a read-only audit for the later66-output effect128 assessment. No actual new checkpoint, assessment plan or result is bound yet. It uses exact copies of the frozen NumPy safe-array reader and analytical score functions, with source hashes in pins.json. The audit itself imports no Torch or native model code.

The audit verifies completed parent/worker/monitor records, the exact source graph and prepared inputs, three checkpoint files,825 original unchanged foundation records, every output file, all66 call identities and the900-second/48GiB host/60GiB reserved CUDA/8GiB available limits. Allocated and reserved counters are separate reads; any allocated-above-reserved observation is reported descriptively and is not treated as an additional runtime cap.

It recomputes12 guided endpoint scores from four held-out noises and three checkpoints, six original k506 own-corruption losses and three original k999 contrast scores. All scores use future latents only. The strict predeclared numerical gate requires the new final checkpoint to have lower contrast MSE than zero and old128, and positive target alignment, on every held-out noise. The1e-12 relative/1e-15 absolute tolerance compares serialized FP64 descriptive numbers only. It never changes the strict gate inequalities.

The12 historical comparisons use the exact saved old20 predictions. New seen506-zero/old128 filenames map to objective-0/128-closed/open; new original999 filenames map to causal-0/128-closed/open-atrium/native_negative. Per-call tensor byte equality, full-file equality, maximum difference and future RMS difference are retained. Any mismatch is labeled unresolved cross-run equivalence. No tolerance is invented, and this descriptive result does not silently change the within-new-run numerical gate.

Seven focused CPU checks passed in cpu-v1.json. They include reading the actual historical12 arrays, independently reconstructing the exact original506 and999 input hashes, known FP32 CFG/endpoint arithmetic, all-four strict gate failures, resource cap failures, future binding rejection and safe paths. Preparation-history retains the missing-pytest launch detail, the temporary macOS symlink fixture rejection and a report-wrapper syntax error. Those did not change runtime logic. The final tests use stdlib unittest and NumPy1.26.4.

After actual training and its independent audit pass, root creates the actual assessment plan. The independent plan review must contain schema worldline-action-effect-assessment-actual-plan-review-v1, status passed, plan_sha256, source_sha256, checkpoint_sha256, new_training_audit_sha256 and counts. Its values must match the actual prepared plan. This review grants no model or provider permission.

Create the later binding from a fresh local prepared copy before execution:

    work/wan-adapter-env/bin/python work/wan22-action-effect-assessment-audit-prep-v1/audit.py --bind --prepared ACTUAL_ASSESSMENT_PREPARED --plan-review ACTUAL_PLAN_REVIEW --output FRESH_BINDING_JSON

The factory checks the actual final training audit, exact training/assessment sources, all original125 reference input files and the four held-out noises. It writes actual input hashes only. Root reviews the created binding checksum. After verified recovery, run:

    work/wan-adapter-env/bin/python work/wan22-action-effect-assessment-audit-prep-v1/audit.py --binding FRESH_BINDING_JSON --binding-sha256 REVIEWED_BINDING_SHA256 --run-root ACTUAL_RECOVERED_ASSESSMENT --recovery-verified ACTUAL_RECOVERY_VERIFIED_JSON --old-comparison work/wan22-action-cuda-post128-recovered-final-v1/recovered/action-results/comparison128-spatial-v1 --output FRESH_AUDIT_DIRECTORY

Every audit result retains its source, input binding and complete before/after recovery inventory. A passing artifact audit and a passing numerical gate remain separate fields. Neither proves visible door motion, camera following or generalization beyond one seen scene. Only the raw66 head outputs are saved; source and counters support the12 feature extractions without a native replay.
