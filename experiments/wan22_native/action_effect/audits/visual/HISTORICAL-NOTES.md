This read-only auditor is prepared for a later completed effect128 visual pair. It does not bind an invented checkpoint or run. The prior final128 auditor is preserved byte-exact under reference/. Array decoding, raw RGB and PNG checks, source-derived baseline conditions, all 100 saved latent states, first-step CFG arithmetic, 825 foundation values and 196 VAE records retain the prior code. The changed arm metadata requires negative-context adapter training and explicitly limits it to the start0 auxiliary; main FM remains positive-only.

Six focused CPU checks passed in cpu-v1.json. They cover a synthetic future-evidence binding, explicit binding checksum, missing audit rejection, incomplete/wrong checkpoint or auxiliary counts, changed files/source rejection, and unchanged numerical audit code. These fixtures do not constitute an actual training or visual result. Training and visual sources remain unchanged.

After a real effect128 training run and its independent audit pass, create the actual visual plan with the already reviewed effect visual runner. The independent actual-plan review must be a JSON object with schema worldline-action-effect-visual-actual-plan-review-v1, status passed, visual_plan_sha256, actual_audit_sha256, final_checkpoint_sha256 and the complete visual source_sha256 mapping. Every value comes from the actual retained files. Extra descriptive review fields are allowed. The frozen training prepared-v3 identity and all 88 source hashes are already pinned in pins.json.

The binding factory reads and hashes the actual files, complete prepared artifacts/evidence and source snapshots. It requires the independent program review, exact visual CPU report, final128 training audit with32 auxiliary updates/128 heads, and exact new-family training contract. It creates one fresh external JSON file. No weights, model, Torch, credentials or cloud APIs are used by this factory. Root reviews and retains that file's SHA256 before the audit.

From the workspace root, use these interfaces after supplying actual paths:

    work/wan-adapter-env/bin/python work/wan22-action-effect-visual-audit-prep-v1/binding.py --prepared ACTUAL_VISUAL_PREPARED --source-review work/wan22-action-effect-visual-independent-review-v1/report.json --plan-review ACTUAL_PLAN_REVIEW --output FRESH_BINDING_JSON

    work/wan-adapter-env/bin/python work/wan22-action-effect-visual-audit-prep-v1/audit.py --binding FRESH_BINDING_JSON --binding-sha256 REVIEWED_BINDING_SHA256 --run-root ACTUAL_RECOVERED_VISUAL --recovery-verified ACTUAL_RECOVERY_VERIFIED_JSON --output FRESH_AUDIT_DIRECTORY

The inherited baseline defaults to the retained original spatial clip. It can be supplied explicitly with --baseline-root. All actual input paths must refer to verified local retained files. A binding is read-only metadata and grants no model or provider admission. The auditor retains its binding and sources with its result.

Only first-step raw velocities are saved by the frozen visual producer. All50 states per arm establish their exact clean prefix, but later100 prediction boundaries per arm rely on reviewed source counters. Raw RGB consistency does not establish correct door or camera response; all-frame visual and paired-target analysis remain separate.
