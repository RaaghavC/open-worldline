# Historical probe-only admission

`executed.json` preserves the parent decision byte-exact. Its SHA256 is
`231be55b177eabcf5419c5ddc1898cc4a25ce6aa05c261f303437876020ea101`.
It authorized only the first two-update optimizer-feasibility probe and explicitly
recorded a failed visual foundation. The run failed before any optimizer update.

`portable.json` changes only the three visual-evidence file paths so they resolve
from this directory to the retained repository images. Its separate hash and every
path change are recorded in the publication manifest. It is not the exact file
passed to the worker. It preserves the assessment prose and all source/data/image
hashes. Neither file admits fixed16, a retry with changed sources, or image-quality
claims. Worker metrics retain their exact executed admission hash and path strings.
