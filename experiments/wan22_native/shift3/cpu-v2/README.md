Retained redundant old-source test run. The preceding edit command used an incorrect working-directory-relative path and failed before changing any file. This report therefore checks the same implementation/test source hashes as CPU-v1 and does not contain the later exact-control metadata hash gate. Its checked source is preserved under ../cpu-v1/checked-source/. CPU-v3 is the current implementation report.
