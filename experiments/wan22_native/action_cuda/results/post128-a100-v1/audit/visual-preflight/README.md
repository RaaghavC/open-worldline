This is the saved-file audit prepared for the final128 matched wait/interact visual pair. It requires the exact reviewed plan, the completed128 training audit and the final checkpoint. It verifies retained source and admission hashes, all825 frozen foundation records, the original196-record decoder, all100 saved latent states and their clean prefixes, first-step CFG arithmetic, and all34 raw RGB frames against their PNGs. Existing sampler source and all measured files remain unchanged.

Only the first-step raw velocities were saved. The audit can recheck all saved times and prefixes, but it cannot independently replay later solver updates or recover unretained velocity tensors. Later prediction counts are checked against the source-bound execution counters. Pixel identity does not establish successful action control. Epicurus is preparing the separate target and cp16 RGB comparison.

Run after root verifies complete recovery, using the exact run and recovery paths supplied then:

```sh
OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 work/wan-adapter-env/bin/python work/wan22-action-cuda-final128-visual-audit-prep-v1/audit.py --run-root "$FINAL128_VISUAL_RUN" --recovery-verified "$FINAL128_RECOVERY_RECORD" --output work/wan22-action-cuda-final128-visual-actual-audit-v1
```

The output must be fresh and outside the measured run and repository. The command does not import Torch, run models or contact a provider. All34 source PNGs remain authoritative; the GIF is a display preview.
