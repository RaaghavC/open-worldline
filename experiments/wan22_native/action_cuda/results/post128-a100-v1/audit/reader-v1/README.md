This reader audits the retained20-prediction comparison after recovery. It accepts the exact prepared plan, unchanged original14 inputs, audited final128 checkpoint, source snapshots and explicit parent decision. Two native predictions are used once; each of checkpoints0,16 and128 contributes two own-corruption k506 objective predictions and four held-input k999 command/text predictions.

The reader independently recomputes six future-only losses, six saved positive/negative command-difference tensors, all three guided command effects using the native FP32 order, descriptive CFG cancellation ratios and four zero/native comparisons. Exact equality is reported separately from the unchanged1e-6 runtime parity bound. It checks825 original foundation records, retained sampled limits, terminal completion and source/output hashes. It does not load models or infer visual quality from these numbers.

```sh
OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 work/wan-adapter-env/bin/python work/wan22-action-cuda-comparison128-audit-prep-v1/audit.py --run "$VERIFIED_COMPARISON128_RUN" --output work/wan22-action-cuda-comparison128-actual-audit-v1
```

Use only the complete run after root verifies recovery. Output must be fresh and outside the run and repository. No cloud calls, credentials, models or training occur. All input files are rehashed afterward. Reported CPU reduction differences use the same descriptive tolerances as the original14 audit; model or parity thresholds are unchanged.
