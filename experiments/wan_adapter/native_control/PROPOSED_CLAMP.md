# Proposed single-factor initial-image clamp diagnostic

Status: this proposal was implemented, independently checked and run once. The [completed result](results/clamp50/README.md) shows repeated surface lattice returning after the clean initial-image clamp. The text below preserves the predeclared comparison. No further generation is implied.

The smallest next comparison adds one treatment to the completed pure T2V control: replace latent frame zero with the independently encoded initial observation before each negative/positive denoiser pair and after each CPU UniPC update. Keep every other tested setting fixed: official source and weight hashes, full float32 arithmetic, CPU UniPC, 50 steps, shift 8, guidance 6, exact positive/native-negative text tensors, 512 x 288 size, 17-frame layout, saved seed-20260908 noise and official float32 VAE decode.

Read only the `observation` tensor from the verified `open-0000` Atrium cache using `safe_open`. Verify the cache manifest, file and tensor hashes and its completed causality checks. Do not materialize future `target` values or action tensors. The input observation was encoded independently from the first original RGB frame. Do not encode a full future clip during this test. Do not attach or load the original adapter.

Retain the exact successful control's initial-noise tensor unchanged as an artifact. Construct the treatment's working latent by applying the frame-zero clamp to a copy. Clamp the input shared by both guidance passes and the updated solver state. Keep the official solver's multistep history and equations otherwise unchanged. Record that this is an explicit image-conditioning adaptation to a T2V foundation, not native pretrained I2V behavior.

The resulting clip contains one reconstruction of the supplied observation and 16 newly generated future frames. Report that count separately from the pure T2V control's 17 generated frames. Retain every frame, both initial noise and clamped initial state, final latents, source snapshots, exact text identities, per-step ranges, memory samples and all failures. Use a new output directory and the existing 900-second, 18 GiB and 2 GiB-available guards. The completed pure T2V run took 663.765 seconds; the added observation tensor is small, but an unchanged budget still must be enforced.

Before execution, independent CPU tests should prove that both text passes receive the same clamped prefix; only frame zero is overwritten; the future latent/noise values remain unchanged before their first solver update; the saved original noise is not mutated; the code cannot load future targets; and all 50 updates retain the prefix. The successful pure T2V implementation and evidence stay unchanged.

Inspect the pair for repeated surface lattice, blur, retention of the supplied initial scene and coherent future appearance. Do not compare the pure T2V frames with renderer truth using PSNR as if they depict the same scene. A single successful clamp clip would show that this specific native-style recipe can tolerate this observation; it would not establish general image continuation, action control or persistent memory. If the lattice returns only with the clamp, the treatment is implicated for this fixed input and seed. If it does not return, the earlier combined numerical/sampling changes remain candidates for the original failure. Further trials require reviewing these frames first.
